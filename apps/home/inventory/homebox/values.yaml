---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/library/common/values.schema.json
controllers:
  homebox:
    containers:
      app:
        image:
          repository: ghcr.io/sysadminsmedia/homebox
          tag: 0.25.0
          pullPolicy: IfNotPresent
        env:
          TZ: "America/Chicago"
          HBOX_OPTIONS_ALLOW_REGISTRATION: "false"
          HBOX_OPTIONS_HOSTNAME: "homebox.mcmahan.io"
          HBOX_OIDC_ENABLED: "true"
          HBOX_OIDC_ISSUER_URL: "https://auth.mcmahan.io/application/o/homebox/"
          HBOX_OIDC_AUTO_REDIRECT: "true"
          HBOX_OPTIONS_TRUST_PROXY: "true"
        envFrom:
          - secretRef:
              name: homebox-oidc

service:
  web:
    enabled: true
    controller: homebox
    type: LoadBalancer
    ports:
      http:
        port: 80
        targetPort: 7745

route:
  homebox:
    hostnames: ["homebox.mcmahan.io"]
    parentRefs:
      - name: remote
        namespace: traefik
        sectionName: websecure
    rules:
      - backendRefs:
          - identifier: web
            port: 80

persistence:
  data:
    enabled: true
    existingClaim: homebox-data
    advancedMounts:
      homebox:
        app:
          - path: /data
