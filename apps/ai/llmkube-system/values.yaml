---
metrics:
  enabled: true

prometheus:
  serviceMonitor:
    enabled: true
  inferencePodMonitor:
    enabled: true

grafana:
  dashboards:
    enabled: true
    operator:
      enabled: false

modelCache:
  enabled: true
  mode: perService
  size: 120Gi
  storageClass: ceph-block
